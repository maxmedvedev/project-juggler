# Kotlin 2.1.0's bundled intellij-core cannot parse Java version strings like "25.0.3",
# so the Gradle/Kotlin daemons must run on JDK 17 regardless of the ambient JAVA_HOME.
org.gradle.java.home=/Users/max/Library/Java/JavaVirtualMachines/azul-17.0.20/Contents/Home